#ifndef _Qlearning_c
#define _Qlearning_c

#include "Qlearning.h"

void Qlearning(InstanceInfo *Info)
{
    //general info
    int Dtn = Info->finish; // destination

    //staring state
    statesinfo PStart;
    PStart.node = Info->start;
    Pstart.Rrhs = Info->
    
    
    PStart.done = true;



    
    while ()
    {

        //Creating path
        int cpath = 0;
        int npath[nx];
        statesinfo *st;

        while(true) // termination condition
        {
            while() //resource cap and reaching to the destination
            {
                
            }
            


        }

        //updating the states
        //updating the qvalues

    }
    


}

#endif